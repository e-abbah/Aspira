import { Invoice } from '../../types/subscription';

// Empty on purpose — matches the "No billing history yet" state in the
// screenshots for a Free-tier user. Populate this once upgrade flows exist.
export const mockInvoices: Invoice[] = [];